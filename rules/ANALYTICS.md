# Analytics Rules

- Events use the typed taxonomy in `src/engines/analytics-engine.ts`.
- Do not send file contents, prompts, answers, secrets, tokens or credentials.
- Analytics is not an authorization, billing or academic-truth source.
- Provider adapters must support no-op/local fallback when external analytics is unavailable.
